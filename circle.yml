machine:
  services:
    - docker

dependencies:
  override:
    - docker build --build-arg BASIC_AUTH_USER=$BASIC_AUTH_USER --build-arg BASIC_AUTH_PASSWORD=$BASIC_AUTH_PASSWORD -t $AWS_ACCOUNT_ID.dkr.ecr.us-east-1.amazonaws.com/apr:$CIRCLE_SHA1 .

test:
  override:
    - echo "bypassing test for now"

deployment:
  prod:
    branch: master
    commands:
      - ./deploy.sh